import React from 'react';
import Example from '../../utilities/title';

const Login = () => {
    Example('D-login')
    return (
        <div>
            <h1>This is a log in section</h1>
        </div>
    );
};

export default Login;